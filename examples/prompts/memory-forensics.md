# Memory Forensics Triage (Volatility3)

## Use Case
Generate Python scripts to triage memory dumps using Volatility3, with iterative error fixing via --loop mode.

## Prompt
```bash
opencode --loop "Write a Python script using Volatility3 to parse {{MEMORY_DUMP}} for {{ARTIFACT_TYPE}} (processes, network connections, injected code, loaded modules, registry hives). Handle common errors (missing plugins, symbol tables, version mismatches). Output markdown triage report with: executive summary, process tree, suspicious processes, network connections, injected code findings, persistence mechanisms, and IOCs extracted."
```

## Example Usage
```bash
opencode --loop "Write a Python script using Volatility3 to parse sample.raw for processes, network connections, injected code, and loaded modules. Handle common errors (missing plugins, symbol tables, version mismatches). Output markdown triage report with: executive summary, process tree, suspicious processes, network connections, injected code findings, persistence mechanisms, and IOCs extracted."
```

## Expected Script Structure
```python
#!/usr/bin/env python3
"""
Volatility3 Memory Triage Script
Auto-generated by SOC-Nemotron-CLI
"""

import volatility3.framework
from volatility3.framework import contexts, automagic, plugins
from volatility3.plugins import linux, windows, mac
import json
import sys

def main():
    dump_path = "sample.raw"
    ctx = contexts.Context()
    
    # Automagic layer detection
    automagics = automagic.available(ctx)
    for automagic_obj in automagics:
        automagic_obj(ctx, dump_path)
    
    # Run plugins
    results = {}
    
    # Process listing
    # Network connections
    # Injected code detection
    # Persistence mechanisms
    
    return results

if __name__ == "__main__":
    main()
```

## --loop Mode Benefits
1. **Auto-fixes** import errors, missing dependencies
2. **Iterates** on Volatility3 plugin API changes
3. **Validates** output against sample dump
4. **Refines** detection logic based on results

## Skill Level
- **Intermediate** - Basic Volatility3 usage
- **Expert** - Custom plugins, kernel internals, rootkit detection